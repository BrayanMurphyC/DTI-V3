= form_for @evento_ponente do |f|
  - if @evento_ponente.errors.any?
    #error_explanation
      %h2= "#{pluralize(@evento_ponente.errors.count, "error")} prohibited this evento_ponente from being saved:"
      %ul
        - @evento_ponente.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :tema
    = f.text_field :tema
  .field
    = f.label :rate
    = f.number_field :rate
  .field
    = f.label :ponente_id
    = f.number_field :ponente_id
  .field
    = f.label :event_id
    = f.number_field :event_id
  .actions
    = f.submit 'Save'
